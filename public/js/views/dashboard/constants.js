// =============================================================================
// public/js/views/dashboard/constants.js
// Shared UI-string constants used across the dashboard module split
// (dashboardView.js + the Strategy classes under dashboard/strategies/).
// =============================================================================

// Shown when closing the quotation form modal (backdrop click, X, Escape)
// while it has unsaved changes — see modalUI.js's registerCloseGuard().
export const DISCARD_QUOTATION_MSG =
  'Tienes cambios sin guardar en esta cotización. ¿Deseas salir y descartarlos?';
